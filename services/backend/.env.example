APP_ENV=local
AWS_REGION=us-east-1
DYNAMODB_TABLE_NAME=fleet-dev-operational
COGNITO_USER_POOL_ID=
COGNITO_REGION=us-east-1
CORS_ORIGINS=http://localhost:5173
# Local dev: set SKIP_JWT_VERIFY=true and use X-Dev-User header JSON
SKIP_JWT_VERIFY=false
# When true, Cognito sends invitation email with temporary password (deployed default)
SEND_INVITE_EMAIL=true
